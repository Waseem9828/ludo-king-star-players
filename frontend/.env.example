# Optional. Leave unset to auto-detect the backend at
# http://<page-host>:5000/api (works for localhost, LAN IP, and phones
# without any edits). Only set this if frontend and backend run on
# different hosts.
# VITE_API_URL=http://192.168.1.4:5000/api
